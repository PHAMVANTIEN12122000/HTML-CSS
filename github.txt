# kiểm tra phiên bản git.
 
 windows + R "cmd"
 
 git --version


# Thiết lập tên và email

 git config --global user.name New_Name --add
 check name : git config user.name

 git config --global user.email email@gmail.com --add
 check email: git config user.email 


# kiểm tra file đã bị thay đổi :

 git status 

 # kiểm tra trạng thái thay đổi cụ thể :
 
 git status -s 
   output:
    M,T : modified
    A :   added 
    D :   deleted
    R :   renamed 
    C :   copied 
    U :   updated but unmerged 



# Git add . [chuyển bị lưu]

 git add .



# Git reset [lấy ra khỏi lệnh chuyển bị lưu ..]
 
 git reset



# Git commit là gì ? các sửa git commit [ chính thức lưu ]
 
 git commit -m" [#key-task] + [A/M/D] + [ feature: tính năng] "

 
 # fix git commit 
 
 git commit --am -m" [#key-task] + [A/M/D] + [ feature: tính năng] "
  


# Git log , git log --oneline  [xem thời điểm git commit]
 
 git log 

 git log --oneline


 # Git checkout id_commit [trở về thời điểm id_commit trước đó]
  
   git checkout id_commit.

 
 # Chuyển về hiện tại.
  
   git checkout branch_name 



# Git Branch [kiểm tra nhánh]
 
 git branch




# Câu lệnh tạo nhánh mới :

 git checkout -b new_name_branch





# Chuyển qua lại giữa các nhánh .
  
  git checkout branch_name 




# Cách kiểm tra nhánh code đang đứng

 git branch --show 



# Git merge branch_name [tổng hợp lại nhánh]

 git merge branch_name



# Git branch -d branch_name_delete [xóa nhánh đã gộp khi xong ko cần thiết nữa]

 git branch -d branch_name_delete 




# Merge code là gì ?
 
 Pull requets -> New pull request -> create pull request -> Merge pull request [leader] -> Comfirm merge [leader] 




# Git pull :  lấy về thông tin từ remote và cập nhật vào các nhánh của local repo

 git pull origin name_branch




# Git push -u origin branch_local_new [đẩy branch [nhánh] mới của local lên remote ]
 
 git push -u origin branch_local_new




# Lấy branch from remote to local [kéo nhánh từ github về local]

 git fetch origin   ->   git checkout -b branch_remote origin/branch_remote



#  .gitignore trong file này xác định thư mục git không được quan tâm tới
 
 đặt tên [file_name]  git không được quan tâm vào đây. -> open terminal -> git add .  ->  git commit -m "gitignore"  ->  git push origin branch  



# Fork ? [ xây dựng với mã nguồn mở .....]
 
 link_author -> git_your -> Fork  -> fix code  ->  Pull request  -> new pull request  -> create pull request X2



# Các bước đẩy code lên github

 git init

 git status

 git add .

 git  commit -m"upload sourd code"

 git remote add origin https://github.com/........git

 git push origin master     [origin được coi là link HTTPS]




# Git clone [download the code from Github to Computer ]

  git clone https:....[in HTTPS]
